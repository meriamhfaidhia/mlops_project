pipeline {
    agent any

    environment {
        PROJECT_DIR = "${WORKSPACE}"  // Utilisation du workspace Jenkins
        MODEL_PATH = "${PROJECT_DIR}/prediction_model.joblib"
    }

    stages {
        stage('Clone Repository') {
            steps {
                script {
                    echo "📥 Clonage du dépôt..."
                    sh "git config --global http.postBuffer 1048576000"  // 1 GB
                    sh """
                        set +e
                        for i in {1..5}; do
                            git clone https://github.com/meriamhfaidhia/mlops_project.git ${PROJECT_DIR} && break
                            echo "Attempt $i failed. Retrying in 5 seconds..."
                            sleep 5
                        done
                        set -e
                    """
                }
            }
        }

        stage('Setup Virtual Environment') {
            steps {
                script {
                    echo "🔄 Installation des dépendances..."
                    sh """
                        set -e
                        cd ${PROJECT_DIR}
                        python3 -m venv venv
                        . venv/bin/activate
                        pip install -r requirements.txt
                    """
                }
            }
        }

        stage('Code Linting') {
            steps {
                script {
                    echo "🔍 Vérification du style de code avec Flake8..."
                    sh """
                        set -e
                        cd ${PROJECT_DIR}
                        . venv/bin/activate
                        flake8 main.py model_pipeline.py --count --show-source --statistics
                    """
                }
            }
        }

        stage('Run Unit Tests') {
            steps {
                script {
                    echo "🧪 Exécution des tests unitaires..."
                    sh """
                        set -e
                        cd ${PROJECT_DIR}
                        . venv/bin/activate
                        pytest tests/ --maxfail=1 --disable-warnings -q
                    """
                }
            }
        }

        stage('Train Model') {
            steps {
                script {
                    echo "🚀 Entraînement du modèle..."
                    sh """
                        set -e
                        cd ${PROJECT_DIR}
                        . venv/bin/activate
                        python3 main.py --train --train_path churn-bigml-20.csv --test_path churn-bigml-80.csv
                    """
                }
            }
        }

        stage('Evaluate Model') {
            steps {
                script {
                    echo "📊 Évaluation du modèle..."
                    sh """
                        set -e
                        cd ${PROJECT_DIR}
                        . venv/bin/activate
                        python3 main.py --evaluate --train_path churn-bigml-20.csv --test_path churn-bigml-80.csv
                    """
                }
            }
        }

        stage('Save Model') {
            steps {
                script {
                    echo "💾 Sauvegarde du modèle..."
                    sh """
                        set -e
                        cd ${PROJECT_DIR}
                        . venv/bin/activate
                        python3 main.py --save ${MODEL_PATH} --train_path churn-bigml-20.csv --test_path churn-bigml-80.csv
                    """
                }
            }
        }

        stage('Deploy API') {
            steps {
                script {
                    echo "🚀 Lancement de l'API FastAPI..."
                    sh "bash ${PROJECT_DIR}/start_api.sh"
                }
            }
        }

        stage('Test API') {
            steps {
                script {
                    echo "✔️ Test de l'API via Swagger..."
                    sh """
                        set -e
                        curl --retry 10 --retry-delay 5 --fail http://192.168.93.6:8000/docs
                        curl --retry 10 --retry-delay 5 --fail http://192.168.93.6:8000/predict -X POST -H "Content-Type: application/json" -d '{"data": [0.1, 0.2, 0.3]}'
                    """
                }
            }
        }

        stage('Stop API') {
            steps {
                script {
                    echo "🛑 Arrêt de l'API FastAPI..."
                    sh "bash ${PROJECT_DIR}/stop_api.sh"
                }
            }
        }
    }

    post {
        always {
            echo "🔧 Nettoyage ou actions post-exécution"
            sh "rm -rf ${PROJECT_DIR}/venv"
        }
        success {
            echo "✅ Pipeline exécuté avec succès!"
        }
        failure {
            echo "❌ Pipeline échoué."
        }
    }
}
